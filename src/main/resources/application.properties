# server.port=0 makes application run on random port
# If not specified the default server port will be 8080
# server.port=8089
# H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
#http://localhost:<server.port>/<context-path>/<h2.console.path>/
#http://localhost:8089/product-store/h2-console/

# Datasource
# default embedded mode
spring.datasource.url=jdbc:h2:file:~/product-db

spring.datasource.username=99x
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql = true
#The base url is no longer http://localhost:8080.
#Now the base url is http://localhost:8080/product-store
server.servlet.context-path=/product-store
